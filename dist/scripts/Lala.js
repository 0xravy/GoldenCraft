class Lala {
    constructor() {}

    invoicesADD() {
        invoicesADD;
    }

    search() {
        // let inputSearch;
        console.log("start search");
        var myMain = mainBonds.children;
        for (let i = 0; i < myMain.length; i++) {
            myMain.item(i).remove();
        }
        console.log("end search");
    }

    more(mainDad) {
        var arrContent = [0, 0, 0, "1444/11/3"];
        var arrClassName = ["user", "mony", "gold", "user"];
        NewDiv(mainDad, arrContent, arrClassName);
    }
}
